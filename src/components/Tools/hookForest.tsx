import { useEffect, useState, useRef } from 'react';

/* Dimension */
export const getWindowDimensionHook = () => {
    // const [refresh, setRefresh] = useState(false);
    // const w = useRef(window.innerWidth), h = useRef(window.innerHeight);
    // const onResizeFunc = () => {
    //     w.current = window.innerWidth;
    //     h.current = window.innerHeight;
    //     setRefresh(!refresh);
    // };
    // useEffect(() => {
    //     window.addEventListener('resize', onResizeFunc);
    //     return () => window.removeEventListener('resize', onResizeFunc);
    // });
    // return { width: w.current, height: h.current };
    return <></>;
};